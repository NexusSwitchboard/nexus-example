import { ModuleConfig } from "@nexus-switchboard/nexus-extend";
declare const _default: (_config: ModuleConfig) => {
    name: string;
    config: {
        CONN_CONFIG: string;
    };
}[];
export default _default;
